let example = 1;

example = example+1;
example++;

example = example+5;

example -= 5;
console.log(example);

// CHALLENGE

const customerAnswer = Math.random() >= 0.5; //return the psudocode number between 0 to 1
let quantity = 2;

if (customerAnswer === true) {

quantity++;
}

console.log(quantity);